name: rename_server
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.1 <3.0.0'


dependencies:
  args: ^2.0.0
  http: ^0.13.4
  path: ^1.8.2
  shelf: ^1.1.0
  shelf_router: ^1.0.0
  xml: ^6.0.1

dev_dependencies:
  lints: ^1.0.0
  test_process: ^2.0.0
  test: ^1.15.0
